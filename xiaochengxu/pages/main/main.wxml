<!--pages/main/main.wxml-->
<import src="/wxSearch/wxSearch.wxml"/> 
<!--<view class="search-container" id="search" >
  <icon bindtap="searchClickEvent" type="search" size="20"/>
  <input id="search_value" type="text" placeholder="{{placehoder}}"/>
</view>
<view class="hotquery" style="{{bvisiable}}">
<label>热门搜索:</label>
  <block wx:for="{{hotword}}" wx:key="item">
  <navigator url="../main/main?keyword={{item}}">
    <label>{{item}}</label>
  </navigator>
  </block>
</view>-->

<view class="wxSearch-section">
  <view class="wxSearch-pancel">
    <input bindinput="wxSearchInput" bindfocus="wxSerchFocus" value="{{wxSearchData.value}}" bindblur="wxSearchBlur" class="wxSearch-input" placeholder="{{placehoder}}" bindconfirm="wxSearchFn"/>
    <button class="wxSearch-button" bindtap="wxSearchFn" size="mini" plain="true">搜索</button>
  </view>
</view>
<template is="wxSearch" data="{{wxSearchData}}" />

<scroll-view scroll-y="true" style="height:{{scrollHeight}}px;display:{{isShow ? 'block':'none'}};" bindscrolltolower="scrollLowerEvent">
  <view class="logo" wx:if="{{!loadingMore && totalRecord == 0 && !isInit}}">
    <icon type="cancel" color="#B0AAAA" size="50" />
    <view>
      <text>没有找到相关数据</text>
    </view>
  </view>
  <view class="header" wx:if="{{totalRecord > 0 && !isInit}}">
    查询到 <text style="color:green">{{totalRecord}}</text> 条数据
  </view>
  
  <view class="common-list" wx:if="{{totalRecord > 0}}">
    <block wx:for="{{pageData}}" wx:key="">
      <navigator url="./detail?instCode={{item.instCode}}">
      <view class="list-item" data-bid="{{item.instCode}}">
        <view class="index-list-item">
          <view class="cover">
            <span>{{item.logoName}}</span>
            <text>{{item.instName}}</text>
          </view>
          <view class="content">
            <!--<block>-->
            <view class="title">
              <text>成立年限: {{item.ratifyDate}} | </text>
              <text>发证量: {{item.authCount}} | </text>
              <text>认证人员数量: {{item.authPersonCount}} </text>
            </view>
            
            <!--</block>-->
            <view class="service-tags">
              <text>{{item.instStatus}}</text>
              <block wx:if = "{{item.cnasRec == '认可'}}">
                <text>CNAS{{item.cnasRec}}</text>
              </block>
              <block wx:if = "{{item.outsideRec == '认可'}}">
                <text>境外{{item.outsideRec}}</text>
              </block>
              <block wx:if = "{{item.highNewTecRec == '获得'}}">
                <text>高新技术企业</text>  
              </block>       
              <text>{{item.instType}}</text>
            </view>
          </view>
        </view>
      </view>
      </navigator>
    </block>
  </view>
  <view class="refresh-footer" wx:if="{{loadingMore}}">
    <icon type="waiting" size="30" color="{{footerIconColor}}" />
  </view>
  
</scroll-view>


